# MAD Event Mailer / MAD 活动邮件系统

插件后台仅对 WordPress 站点管理员和“邮箱管理员”角色的用户显示并开放使用。

[English README](./README.md)

## 下载最新版

GitHub Releases 已启用，用于发布版本信息。最新版正式插件请从官方 [WordPress.org 插件页面](https://wordpress.org/plugins/mad-event-mailer/) 下载。

MAD Event Mailer 是一个用于 WordPress 的活动通知邮件插件，适合比赛、活动、投稿审核、成绩通知、赛程通知、结果通知等场景。

插件支持 SMTP 发信、HTML 邮件模板、模板变量、CSV 收件人导入/导出、活动订阅列表、短代码前台订阅表单、批量发送和定时发送。2.3.1 起，后台及前台订阅界面以英文作为源码语言，并使用 WordPress 标准翻译系统。

## 项目信息

- **插件名称：** MAD Event Mailer
- **作者：** [MAD Producer Studio](https://github.com/MAD-Producer)
- **许可证：** GPL v2
- **Text Domain：** `mad-event-mailer`
- **当前版本：** 2.4.4
- **短代码：** `[madevma_email_register]`

## 主要功能

### SMTP 发信

可以在后台配置第三方 SMTP 服务，包括：

- SMTP 地址
- SSL/TLS 协议
- 端口
- 邮箱账号
- 邮箱密码
- 发件邮箱
- 发件人姓名
- 回复地址
- 每批发送数量
- 邮件之间的后台发送间隔

适合使用独立活动邮箱发送通知邮件。

### HTML 邮件模板

插件支持可复用的 HTML 邮件模板：

- 默认中文模板
- 默认英文模板
- 上传自定义 HTML 模板
- 直接粘贴 HTML
- 后台预览模板
- 基于通用模板快速创建新模板

默认通用模板会被锁定，不可删除。

### 模板变量

变量统一使用双大括号格式：

```text
{{变量名}}
```

常用内置变量：

| 变量 | 含义 |
| --- | --- |
| `{{title}}` / `{{title1}}` | 邮件标题 |
| `{{name}}` / `{{name1}}` | 收件人姓名 |
| `{{email}}` | 收件人邮箱 |
| `{{message}}` / `{{message1}}` | 正文内容插槽 |
| `{{unsubscribe_url}}` | 订阅管理页面链接 |

正文里也可以写自定义变量，例如：

```text
你的分数是 {{score}}
你的排名是 {{rank}}
评语：{{comment}}
```

如果 CSV 中包含 `score`、`rank`、`comment` 这些列，每个收件人就会收到自己对应的内容。

### 正文插槽编辑

通用 HTML 模板可以保持不变，只把 `{{message1}}` 当作正文插槽。

例如模板里写：

```html
<div class="personal-message">{{message1}}</div>
```

发送邮件时，只需要在后台富文本编辑器里编辑正文内容，不需要每次都修改完整 HTML 模板。

### CSV 收件人模板导出

插件可以根据当前模板和正文中使用的变量，导出对应的 CSV 模板。

示例：

```csv
email,name,events,score,rank,comment
john@example.com,John,IFT IC #6,95,2,Good work
```

基本字段：

- `email`
- `name`

可选字段：

- `events`
- `template`
- 正文或模板中使用的自定义变量

### 在线收件人编辑

收件人页面新增了按行编辑的表格。每行必须绑定一个具体邮件模板，也可以绑定一个或多个活动语言分组。选择模板后，系统会自动识别该模板中的可编辑变量并生成普通输入框，不再需要手写 JSON；模板和收件人变量会在发送时严格按绑定关系使用。明显的广告收件人会在前台订阅、手动添加、CSV 导入、在线编辑、创建发送任务和实际发送时被丢弃，已有数据也可以在收件人页面批量清理。

### 活动订阅列表

后台可以创建和删除活动分类。用户可以选择自己想接收哪些活动通知。

适合：

- 比赛通知
- 投稿审核结果
- 赛程变更
- 获奖结果
- 社群活动通知

### 前台订阅表单

创建一个 WordPress 页面，并加入短代码：

```text
[madevma_email_register]
```

前台表单支持：

- 订阅活动通知
- 查询当前订阅状态
- 退订所有通知

订阅逻辑是增量式的：用户再次订阅时，只会增加新的活动分类，不会删除之前已经订阅的分类。

退订则表示全部退订。

### 邮件底部订阅管理按钮

插件可以自动在邮件底部追加“订阅管理 / 退订”按钮。

可以在：

- 全局 SMTP 设置
- 单次发送任务设置

中控制是否添加按钮。

按钮支持中文和英文两种语言。

### 批量发送和定时发送

插件支持后台分批发送，减少服务器压力。后台可以设置每批上限和邮件之间的发送间隔。

发送任务支持：

- 立即发送
- 定时发送
- 保存草稿
- 调用旧任务设置继续编辑
- 在排队、定时或发送中的状态下取消任务

创建任务后不会在当前后台请求中直接发送，而是交给 WordPress Cron 后台 worker。设置发送间隔后，worker 每次最多发送一封，再按间隔调度下一封；每个收件人发送前都会检查任务是否已取消。取消任务后，待发送收件人会停止处理。WP-Cron 仍受站点访问量和 Cron 配置影响，实际执行时间可能有延迟。

### 邮件品牌图片

SMTP 设置页提供邮件 Logo 和底部 Icon 两个图片链接配置项。内置模板通过 `{{logo_url}}` 与 `{{icon_url}}` 变量读取，不再包含写死的远程图片依赖；对应设置留空时不会显示图片。

插件后台和前台订阅界面使用英文源码文字，并通过 `mad-event-mailer` text domain 与 WordPress gettext 函数支持翻译。发行包不附带翻译文件，社区翻译通过 translate.wordpress.org 管理。

## 安装方式

### 通过 ZIP 安装

1. 下载插件 ZIP。
2. 进入 WordPress 后台 → 插件 → 安装插件 → 上传插件。
3. 上传 ZIP 文件。
4. 启用插件。
5. 进入后台左侧的 **MAD 邮件**。
6. 先配置 SMTP，再进行发送。

### 通过源码安装

将插件文件夹复制到：

```text
wp-content/plugins/mad-event-mailer/
```

然后在 WordPress 后台启用插件。

## 推荐配置流程

1. 配置 SMTP。
2. 新建订阅管理页面，页面内容放：

   ```text
   [madevma_email_register]
   ```

3. 将该页面链接填入插件设置。
4. 创建活动分类。
5. 手动添加收件人、导入 CSV，或让用户从前台订阅。
6. 选择或创建邮件模板。
7. 编辑邮件正文。
8. 如果需要个性化变量，导出 CSV 模板并填写。
9. 先发送测试邮件。
10. 创建正式发送任务。

## 模板写法规则

变量格式：

```text
{{变量名}}
```

正确示例：

```text
{{name1}}
{{message1}}
{{score}}
{{rank}}
{{comment}}
```

不推荐：

```text
{{ score }}
{{user name}}
```

推荐通用模板结构：

```html
<h1>{{title1}}</h1>
<p>Dear {{name1}},</p>
<div>{{message1}}</div>
```

个性化正文示例：

```text
Dear {{name1}},

Your score for IFT IC #6 is {{score}}.
Your ranking is {{rank}}.

Comment:
{{comment}}
```

CSV 示例：

```csv
email,name,events,score,rank,comment
john@example.com,John,IFT IC #6,95,2,Excellent work
jane@example.com,Jane,IFT IC #6,88,5,Good structure
```

## 注意事项

- 定时发送依赖 WordPress Cron。
- 大量发送时可能受到 SMTP 服务商限制。
- 正式群发前建议先用小范围收件人测试。
- 建议为发件域名配置 SPF、DKIM、DMARC，提高送达率。
- 插件用于活动通知和社群通知，不应用于垃圾邮件或未经许可的营销邮件。

## Release 说明

### 2.4.4

- 修复活动邮件在活动语言列表缺失或无效时退化为全体订阅者的问题。
- 将全体订阅者发送改为必须明确选择并确认的选项，并在任务记录中显示实际收件来源。
- 保存 WordPress/SMTP 返回的失败原因；当没有任何邮件被接受时，任务标记为失败而不是已完成。
- 将严格绑定邮件模板的在线收件人编辑器移到与导入/导出并列的“收件人数据工具”区域，新行必须明确选择模板。

### 2.4.3

- 将发送任务改为后台 WP-Cron worker，创建任务时不会阻塞当前后台请求。
- 新增每封邮件发送间隔和安全批量上限，降低服务器压力。
- 新增取消发送任务，排队、定时和发送中的任务都可以停止待发送收件人。
- 新增 worker 锁和逐收件人取消检查，避免任务重复并发发送。

### 2.4.2

- 发送前将模板 CSS 转为内联样式，避免 Gmail 等会移除 style 标签的客户端丢失邮件布局。
- 通过 PHPMailer 明确将外发邮件配置为 UTF-8 HTML 邮件。
- “从通用模板创建”仅允许使用内置中英文通用模板，避免误继承之前创建的模板内容。

### 2.4.1

- 将在线收件人变量编辑从 JSON 改为邮件模板驱动的字段编辑。
- 在线编辑中的每一行必须绑定一个有效邮件模板。
- 选择模板后自动识别其中的可编辑变量，并在切换模板时重建变量字段。
- 对模板绑定或收件人数据无效而跳过的在线行给出明确提示。

### 2.4.0

- 更新 WordPress.org 元数据，标记 `Tested up to: 7.1`。
- 新增明显广告收件人过滤，覆盖收件人录入和邮件发送链路。
- 新增在线收件人表格，支持活动语言分组和按收件人绑定邮件模板。
- 新增按收件人保存模板变量，并同步发送逻辑使用绑定模板。
- 收件人模板 CSV 新增 `template` 列。

### 2.3.1

- 后台与前台订阅界面改用英文源码文字。
- 移除自定义翻译辅助函数，全面改用 WordPress gettext 函数和 `mad-event-mailer` text domain。
- JavaScript 动态提示通过 WordPress 本地化传递，备用文案统一改为英文。
- 将内置模板名称迁移为英文，同时兼容已有 2.3.0 数据。
- 修正 WordPress.org 标签数量和短描述长度。

### 2.3.0

- 在设置页新增 Logo 与 Icon 图片链接，并将模板中的固定图片地址替换为 `{{logo_url}}` 和 `{{icon_url}}`。
- 将公开钩子、选项、角色、权限、短代码、菜单 slug、数据库表、表单字段及资源选择器统一迁移到 `madevma` 唯一前缀。
- 为已有插件数据和短代码页面增加一次性自动迁移。
- 按 WordPress.org 要求移除随包翻译文件，插件界面默认保留中文。
- 更新 WordPress.org 提交包和 GitHub Release 安装包结构。

### 2.2.4

- 修复发出的邮件仍显示 no reply 的问题，发件人名称会稳定使用 SMTP 设置中的发件人姓名。
- 订阅确认和退订确认邮件改为使用通用 HTML 邮件模板。

### 2.2.2

本次 release 延续 2.2.1 的 GitHub 发布元信息更新，并新增以下更新说明：

- 完善英文语言包，补充后台、发送任务、模板、收件人、预览和 JavaScript 状态提示中仍显示中文的文案。
- 后台界面语言和前台订阅页语言默认改为跟随 WordPress 站点语言。

### 2.2.1

- 更新用于 GitHub release 的插件元信息。
- 增加 GPL v2 许可证头。
- 作者信息更新为 MAD Producer Studio，并增加 GitHub 作者链接。
- 增加英文 README 和中文 README。

## 许可证

本项目使用 **GPL v2** 许可证。

详情见 [LICENSE](./LICENSE)。

## 作者

由 [MAD Producer Studio](https://github.com/MAD-Producer) 创建和维护。
